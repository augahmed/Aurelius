# Aurelius Contributor Notes

- Keep packages small, focused, and testable.
- Run `gofmt` on all Go files before finishing.
- Run `go test ./...` before finishing.
- Prefer simple, correct implementations before optimization.
- Do not add heavyweight dependencies without explaining why.
- Document architectural decisions in `docs/`.
